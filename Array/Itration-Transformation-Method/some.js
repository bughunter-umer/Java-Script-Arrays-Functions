// some() → At least one matches

// Returns true if at least one element matches a condition.

let numbers = [5, 10, 15];
console.log(numbers.some(num => num > 10)); // true


// ✅ Like saying: “Does any element match?”