//forEach() → Loop through array

//Executes a function for each element (does not return a new array).

let numbers = [1, 2, 3];
numbers.forEach(num => console.log(num * 2));
// Output: 2, 4, 6


//✅ Good for looping, but cannot return a transformed array.