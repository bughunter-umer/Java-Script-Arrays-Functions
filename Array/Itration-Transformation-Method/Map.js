//map() → Transform array

//Creates a new array by applying a function to each element.

let numbers = [1, 2, 3];
let doubled = numbers.map(num => num * 2);
console.log(doubled); // [2, 4, 6]


//✅ Best when you want to transform values